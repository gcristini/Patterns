/* similar to Shape.java but has differente signatures. so we want to ovverride it
and use only Shape.java*/
public interface GeometricShape {
    public double getPerimeter();
    public double getArea();
}
